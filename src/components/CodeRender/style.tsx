import { styled } from '@mui/material';

export const CodeStyle = styled('code')``;

export const Pre = styled('pre')`
	code {
		margin: 0;
		border-radius: 6px;
	}
	position: relative;
	border-radius: 6px;
	margin: 0;
	height: auto;
`;
